# CryptoBot — Telegram-бот для отслеживания криптовалют

Это раздел для разработчиков и пользователей, которые хотят создать Telegram-бота для наблюдения за ценами криптовалют, трендами и управления ценовыми алертами. Бот работает с помощью модуля Aiogram на языке Python и использует SQLite для хранения пользовательских данных и алертов. CoinGecko собирает данные о ценах, рыночной капитализации и трендах криптовалют через публичное API.

## 1. Требования и установка

Прежде чем начать, убедитесь, что установлена **версия Python 3.8 или выше** на вашем компьютере. Вы можете загрузить Python с официального сайта: https://www.python.org/, а затем следовать инструкциям по установке для вашей операционной системы.

Когда установка Python завершена, откройте терминал или командную строку и выполните команды:

### 1.1 Создание виртуального окружения (рекомендуется)

```bash
python -m venv .venv
# На Windows:
.venv\Scripts\activate
# На Linux/Mac:
source .venv/bin/activate
```

### 1.2 Установка зависимостей

```bash
pip install -r requirements.txt
```

**Основные библиотеки:**

- **aiogram** (версия 3.x) — асинхронный фреймворк для Telegram ботов
- **aiohttp** — асинхронный HTTP-клиент для запросов к API
- **python-dotenv** — для безопасного хранения конфиденциальных данных (токены, ключи API)
- **sqlite3** — встроенная база данных (уже в Python)

**Зависимости:**
- `aiogram`: используется для создания и управления Telegram-ботом
- `aiohttp`: используется для асинхронных HTTP-запросов к CoinGecko API
- `python-dotenv`: используется для безопасного хранения чувствительных данных в файле `.env`

## 2. Создание Telegram-бота

Чтобы создать собственного Telegram-бота, необходимо зарегистрироваться с **BotFather**:

1. Найдите бота в Telegram под названием **@BotFather** и начните общаться
2. Следуйте инструкциям после ввода команды `/newbot`
3. Введите название бота (например, `CryptoTracker`)
4. Введите уникальное имя бота в формате `@username` (например, `@my_crypto_bot`)
5. После создания бота вы получите **TOKEN** в формате: `123456789:ABCDEF1234567890abcdef1234567890abc`

### 2.1 Сохранение токена в `.env`

В корне проекта создайте файл `.env` и добавьте:

```env
TELEGRAM_TOKEN=YOUR_TELEGRAM_TOKEN
```

Файл `.env` уже добавлен в `.gitignore` и не будет загружен в репозиторий, что обеспечивает безопасность ваших учётных данных.

## 3. Настройка CoinGecko API

### 3.1 Создание аккаунта и получение API-ключа

1. Перейдите на https://www.coingecko.com/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **API** (https://www.coingecko.com/en/api)
4. Нажмите **"Create API Key"** для получения Demo API Key (бесплатно)
5. Скопируйте полученный ключ

### 3.2 Сохранение API-ключа пользователем

Каждый пользователь бота должен сохранить свой CoinGecko API-ключ при первом использовании:

1. Запустите бота
2. Отправьте команду `/start`
3. Нажмите кнопку **"🧑‍💻 Вход/Регистрация"**
4. Введите ваш CoinGecko Demo API Key
5. Бот сохранит ключ в локальной базе данных SQLite (`users.db`)

**Примечание:** API-ключ сохраняется в SQLite и используется для авторизованных запросов к CoinGecko. Публичные методы работают без ключа.

## 4. Запуск бота

Бот готов к запуску после установки зависимостей и токена. В каталоге проекта выполните команду:

```bash
python bot.py
```

В терминале появится сообщение о готовности бота. Запустите Telegram, найдите своего бота (по имени из BotFather) и отправьте команду `/start`.

## 5. Доступные команды

После регистрации (ввода API-ключа) вам доступны:

### Цены и конвертация
- `/price <монета> <валюта>` — показать цену монеты
  - Пример: `/price bitcoin usd`
- `/convert <из> <в> <количество>` — конвертация между криптовалютами
  - Пример: `/convert bitcoin tether 0.5`

### Информация о рынке
- `⭐ Топ 10` — показать топ-10 криптовалют по рыночной капитализации
- `/coin <id>` — подробная информация о монете (цена, cap, объём, описание)
  - Пример: `/coin bitcoin`
- `🔥 Тренды` — трендовые монеты на CoinGecko

### Управление алертами
- `/alert <монета> <выше/ниже> <значение> [валюта]` — создать ценовой алерт
  - Пример: `/alert bitcoin выше 90000 usd`
- `🔔 Мои алерты` — показать список ваших активных алертов
- `/alert_remove <id>` — удалить алерт по ID

## 6. Архитектура проекта

```
CryptoBot/
├── bot.py                 # Главный файл запуска
├── handlers.py            # Обработчики команд (aiogram Router)
├── database.py            # Работа с SQLite (сохранение ключей, алертов)
├── alerts.py              # Фоновый worker для проверки алертов
├── http_client.py         # Асинхронный HTTP-клиент с кешированием
├── keyboards.py           # Inline и reply клавиатуры для Telegram
├── coingecko/
│   ├── coingecko_api.py   # Класс CoinGeckoAPI (методы для работы с API)
│   ├── coingecko_client.py # Инициализация CoinGeckoClient
│   └── __init__.py
├── utils/
│   ├── utils.py           # Вспомогательные функции (форматирование цен)
│   └── cache.py           # Кеширование HTTP-ответов с TTL
├── .env                   # Конфиденциальные данные (НЕ коммитить!)
├── .gitignore             # Исключение .env, __pycache__, *.db
└── requirements.txt       # Зависимости проекта
```

## 7. Как работает система алертов

Алерты проверяются в фоновом режиме каждые **15 секунд**:

1. **Фоновый worker** (`alerts.py`) читает список всех алертов из SQLite
2. Для каждого алерта получает текущую цену через CoinGecko API
3. Проверяет условие: цена выше/ниже порога
4. Если условие выполнено и алерт ещё не срабатывал — **отправляет сообщение** пользователю в Telegram
5. Помечает алерт как сработавший в базе данных

**Проверка происходит асинхронно и не блокирует основной цикл бота.**

## 8. Рекомендации при запуске

- **Интернет-соединение**: убедитесь, что ваше интернет-соединение стабильно, так как бот взаимодействует с серверами Telegram и CoinGecko
- **SQLite база данных**: файл `users.db` создаётся автоматически при первом запуске в корне проекта
- **Удалённый сервер**: если бот запущен на удалённом сервере, используйте `screen` или `tmux` для предотвращения завершения процесса при закрытии терминала:
  ```bash
  screen -S cryptobot
  python bot.py
  # Ctrl+A, затем D для отключения
  screen -r cryptobot  # Для подключения обратно
  ```
- **Логирование**: бот логирует все события (ошибки, запросы) с уровнем DEBUG в консоль

## 9. Потенциальные ошибки и их устранение

### Ошибка: "ModuleNotFoundError: No module named 'aiogram'"
**Решение:** убедитесь, что вы установили зависимости в правильном виртуальном окружении:
```bash
pip install -r requirements.txt
```

### Ошибка: "Invalid token provided"
**Решение:** проверьте, что ваш TELEGRAM_TOKEN в `.env` введён **точно** без пробелов и в правильном формате. Любая опечатка приведёт к ошибке.

### Ошибка: "401 Unauthorized" при работе с CoinGecko API
**Решение:** убедитесь, что:
1. Вы сохранили правильный CoinGecko API-ключ в боте (команда `🧑‍💻 Вход/Регистрация`)
2. API-ключ активен на сайте https://www.coingecko.com/en/api/documentation
3. Ключ не истёк (demo-ключи имеют лимиты на количество запросов)

### Ошибка: "Алерты не отправляются"
**Решение:** проверьте:
1. Запущен ли фоновый worker (`alerts.py` должен работать в фоне)
2. Ваш пользовательский ID сохранён в базе данных после регистрации
3. Алерт правильно сохранён в таблице `alerts` в `users.db`
4. Посмотрите логи в консоли для поиска ошибок

Используйте для проверки:
```bash
sqlite3 users.db "SELECT * FROM alerts WHERE user_id=YOUR_USER_ID;"
```

### Ошибка: "Монета не найдена"
**Решение:** CoinGecko может использовать другие названия для монет. Попробуйте:
- Использовать полное имя: `bitcoin` вместо `btc`
- Проверить доступные монеты через `/trending` или `/top`
- Убедиться, что название на англ. языке в нижнем регистре

### Ошибка: "Rate limit exceeded"
**Решение:** demo-ключ CoinGecko имеет лимит запросов. Если видите эту ошибку:
1. Подождите несколько минут перед следующим запросом
2. Обновите API-ключ на https://www.coingecko.com/en/api
3. Используйте встроенный кеш (HTTP-ответы кешируются на 10 секунд)

## 10. Улучшения и расширения

В будущем планируются:
- **AI-помощник** для анализа рынка и ответов на вопросы
- **Более частые проверки алертов** (настраиваемый интервал)
- **Поддержка дополнительных валют** и рынков
- **История цен** и графики в боте
- **Экспорт данных** в CSV/JSON

## 11. Техническая документация

### 11.1 Структура проекта

# Структура проекта
```
CryptoBot/
├── coingecko/
│   ├── __init__.py             # Инициализация пакета
│   ├── coingecko_api.py        # Класс-обёртка для работы с CoinGecko API (цены, тренды, конвертация и др.)
│   ├── coingecko_client.py     # Низкоуровневый HTTP-клиент для CoinGecko
├── utils/
│   ├── cache.py                # Кэширование данных (например, цен)
│   ├── utils.py                # Вспомогательные функции
├── .env                        # Переменные окружения (ключи, настройки)
├── .gitignore                  # Игнорируемые файлы для git
├── alerts.py                   # Управление созданием и отправкой алертов пользователям
├── bot.py                      # Основной файл запуска Telegram-бота, инициализация и роутинг команд
├── database.py                 # Работа с базой данных (SQLite): пользователи, алерты, ключи
├── handlers.py                 # Обработчики команд и кнопок Telegram-бота
├── http_client.py              # Асинхронный HTTP-клиент для запросов к внешним API
├── keyboards.py                # Описание клавиатур для Telegram-бота
├── Readme.md                   # Документация проекта
```

### 11.2 Модуль `bot.py`

**Назначение:** Точка входа в приложение. Инициализирует бота, диспетчер, подключает роутер обработчиков и запускает фоновый worker для алертов.

**Основные компоненты:**
```python
bot = Bot(os.getenv("TELEGRAM_TOKEN"))  # Инициализация бота с токеном из .env
dp = Dispatcher()                        # Диспетчер для обработки событий
dp.include_router(router)                # Подключение роутера из handlers.py
```

**Асинхронная функция `main()`:**
1. Инициализирует базу данных SQLite через `init_db()`
2. Запускает фоновую задачу `alerts_worker(bot)` для проверки алертов
3. Начинает polling (опрос сервера Telegram на наличие новых сообщений)

**Логирование:**
- Уровень DEBUG для детальной диагностики
- Отдельные уровни INFO для aiogram и aiohttp (уменьшение шума в логах)

### 11.3 Модуль `handlers.py`

**Назначение:** Содержит все обработчики команд и callback-кнопок Telegram.

**Основные обработчики команд:**

| Команда | Функция | Описание |
|---------|---------|----------|
| `/start` | `cmd_start()` | Приветствие и отображение главной клавиатуры |
| `/help` | `cmd_help()` | Справка по доступным командам |
| `/about` | `cmd_about()` | Информация о боте |
| `/price <coin> <vs>` | `cmd_price()` | Получение цены криптовалюты |
| `/convert <from> <to> <amount>` | `cmd_convert()` | Конвертация между криптовалютами |
| `/coin <id>` | `cmd_coin()` | Детальная информация о монете |
| `/alert <coin> <direction> <threshold> [currency]` | `cmd_alert()` | Создание ценового алерта |
| `/alert_remove <id>` | `cmd_alert_remove()` | Удаление алерта по ID |

**Обработчики кнопок (Callback):**

| Текст кнопки | Функция | Действие |
|--------------|---------|----------|
| `⭐ Топ 10` | `handle_top()` | Топ-10 криптовалют по капитализации |
| `🔥 Тренды` | `handle_trending()` | Трендовые монеты |
| `🔔 Мои алерты` | `handle_my_alerts()` | Список активных алертов пользователя |
| `💰 Курсы криптовалют` | `handle_currencies()` | Inline-кнопки для выбора валюты |

**FSM (Finite State Machine):**
- Используется для управления состояниями диалога
- Основное состояние: `waiting_for_api_key` — ожидание ввода API-ключа при регистрации

### 11.4 Модуль `database.py`

**Назначение:** Управление SQLite базой данных для хранения пользовательских данных.

**Таблицы:**

**1. `coingecko`** — хранение API-ключей пользователей
```sql
CREATE TABLE IF NOT EXISTS coingecko (
    user_id INTEGER PRIMARY KEY,
    api_key TEXT
);
```

**2. `alerts`** — хранение ценовых алертов
```sql
CREATE TABLE IF NOT EXISTS alerts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    coin TEXT NOT NULL,
    direction TEXT NOT NULL,    -- 'above' или 'below'
    threshold REAL NOT NULL,
    currency TEXT NOT NULL DEFAULT 'usd',
    triggered INTEGER NOT NULL DEFAULT 0
);
```

**Основные функции:**

| Функция | Параметры | Возвращаемое значение | Описание |
|---------|-----------|----------------------|----------|
| `init_db()` | - | - | Создаёт таблицы при первом запуске |
| `save_cg_key(user_id, api_key)` | user_id, api_key | - | Сохраняет API-ключ пользователя |
| `get_cg_key(user_id)` | user_id | str или None | Получает API-ключ пользователя |
| `add_alert(user_id, coin, direction, threshold, currency)` | все параметры | alert_id | Создаёт новый алерт |
| `list_alerts_db()` | - | list of tuples | Список всех алертов |
| `get_user_alerts(user_id)` | user_id | list of tuples | Алерты конкретного пользователя |
| `remove_alert(alert_id, user_id)` | alert_id, user_id | - | Удаление алерта |
| `set_alert_triggered(alert_id, value)` | alert_id, value (True/False) | - | Отметка о срабатывании алерта |

### 11.5 Модуль `alerts.py`

**Назначение:** Фоновый worker для периодической проверки ценовых алертов.

**Принцип работы:**

```python
async def alerts_worker(bot):
    while True:
        # 1. Получить все алерты из БД
        alerts = list_alerts_db()
        
        # 2. Для каждого алерта:
        for alert in alerts:
            # - Проверить, не сработал ли уже
            if triggered:
                continue
            
            # - Получить API-ключ пользователя
            key = get_cg_key(user_id)
            
            # - Запросить текущую цену
            price_data = await api.price(coin, currency)
            
            # - Проверить условие (выше/ниже порога)
            if direction == "above" and price >= threshold:
                await bot.send_message(user_id, ...)
                set_alert_triggered(alert_id, True)
        
        # 3. Подождать 15 секунд перед следующей проверкой
        await asyncio.sleep(15)
```

**Интервал проверки:** 15 секунд (настраивается в `asyncio.sleep(15)`)

### 11.6 Модуль `http_client.py`

**Назначение:** Универсальный асинхронный HTTP-клиент с встроенным кешированием.

**Класс `HTTPClient`:**

**Параметры инициализации:**
- `base_url` — базовый URL API (например, `https://api.coingecko.com/api/v3`)

**Методы:**

| Метод | Параметры | Описание |
|-------|-----------|----------|
| `init()` | - | Инициализирует aiohttp сессию |
| `close()` | - | Закрывает сессию |
| `request(method, path, params)` | HTTP метод, путь, параметры | Универсальный запрос |
| `get(path, params)` | путь, параметры | GET запрос |
| `post(path, params)` | путь, параметры | POST запрос |
| `put(path, params)` | путь, параметры | PUT запрос |
| `delete(path, params)` | путь, параметры | DELETE запрос |

**Кеширование:**
- Использует `CacheTTL` (TTL = 10 секунд по умолчанию)
- Ключ кеша: `{метод}:{url}:{параметры}`
- Автоматически возвращает закешированные данные при повторных запросах

### 11.7 Модуль `coingecko/coingecko_api.py`

**Назначение:** Обёртка для работы с CoinGecko API.

**Класс `CoinGeckoAPI`:**

**Методы API:**

| Метод | Параметры | Endpoint | Описание |
|-------|-----------|----------|----------|
| `price(coin, vs)` | coin_id, валюта | `/simple/price` | Получить цену монеты |
| `list_coins()` | - | `/coins/list` | Список всех доступных монет |
| `get_currencies()` | - | `/simple/supported_vs_currencies` | Поддерживаемые валюты |
| `get_trending()` | - | `/search/trending` | Трендовые монеты |
| `get_markets(vs_currency, per_page, page)` | валюта, кол-во, страница | `/coins/markets` | Топ монет по капитализации |
| `get_coin(coin_id)` | ID монеты | `/coins/{id}` | Детальная информация о монете |
| `get_chart(coin, currency, days)` | coin_id, валюта, дни | `/coins/{id}/market_chart` | Исторические данные цен |
| `convert(from_coin, to_coin, amount)` | из, в, количество | Комбинация `/simple/price` | Конвертация между криптовалютами |

**Аутентификация:**
```python
def auth(self):
    return {"x_cg_demo_api-key": self.api_key}
```
API-ключ передаётся в параметрах каждого запроса.

### 11.8 Модуль `coingecko/coingecko_client.py`

**Назначение:** Специализированный клиент для CoinGecko, наследуется от `HTTPClient`.

```python
class CoinGeckoClient(HTTPClient):
    def __init__(self):
        super().__init__("https://api.coingecko.com/api/v3")

cg_client = CoinGeckoClient()  # Глобальный экземпляр
```

### 11.9 Модуль `utils/cache.py`

**Назначение:** Простое кеширование с TTL (Time To Live).

**Класс `CacheTTL`:**

```python
class CacheTTL:
    def __init__(self, ttl=10):  # TTL в секундах
        self.ttl = ttl
        self.cache = {}
    
    def get(self, key):
        # Возвращает значение, если не истекло TTL
        
    def set(self, key, value):
        # Сохраняет значение с текущей меткой времени
```

**Использование:**
- Уменьшает количество запросов к CoinGecko API
- Ускоряет ответы бота на повторные запросы
- Снижает вероятность превышения rate limit

### 11.10 Модуль `utils/utils.py`

**Назначение:** Вспомогательные функции для форматирования и валидации.

**Функции:**

| Функция | Параметры | Возвращает | Описание |
|---------|-----------|------------|----------|
| `format_price(x)` | float | str | Форматирование цены (до 3 знаков после запятой) |
| `validate_currency(currency)` | str | bool | Проверка, является ли валюта поддерживаемой фиатной |

**Поддерживаемые валюты:**
```python
VALID_FIAT = {
    "usd", "eur", "rub", "kzt", "gbp",
    "uah", "byr", "cny", "jpy", "krw",
    "try", "aud", "cad", "chf", "pln"
}
```

### 11.11 Модуль `keyboards.py`

**Назначение:** Определение клавиатур Telegram (Reply и Inline).

**Reply клавиатуры:**

**1. `main_kb`** — главное меню:
```python
[🔔 Мои алерты] [⭐ Топ 10]
[🔥 Тренды] [💰 Курсы криптовалют]
```

**2. `auth_kb`** — меню регистрации:
```python
[🧑‍💻 Вход/Регистрация]
```

**Inline клавиатуры:**

Генерируются динамически для:
- Выбора валюты (из списка `VS_LIST`)
- Просмотра информации о монетах
- Управления алертами

**Список валют для inline-кнопок:**
```python
VS_LIST = ["usd", "eur", "rub", "kzt", "cny", "try", "gbp", "uah", "jpy", "cad", "chf", "brl"]
```

### 11.12 Диаграмма потока данных

```
┌──────────────┐
│   Telegram   │
│    User      │
└──────┬───────┘
       │ Отправка команды
       ▼
┌──────────────────┐
│   handlers.py    │ ◄── Обработчик команд
│   (Router)       │
└──────┬───────────┘
       │
       ├─► database.py ◄── Сохранение/чтение данных
       │   (SQLite)
       │
       ├─► coingecko_api.py ◄── Запросы к CoinGecko
       │   │
       │   └─► http_client.py ◄── HTTP запросы + кеш
       │       │
       │       └─► cache.py (TTL кеш)
       │
       └─► bot.send_message() ◄── Ответ пользователю
```

**Фоновый процесс алертов:**
```
┌──────────────┐
│ alerts.py    │
│ (worker)     │
└──────┬───────┘
       │ Каждые 15 секунд
       │
       ├─► database.py ◄── Получить алерты
       │
       ├─► coingecko_api.py ◄── Проверить цены
       │
       └─► bot.send_message() ◄── Уведомление пользователя
```

### 11.13 API Endpoints CoinGecko

**Базовый URL:** `https://api.coingecko.com/api/v3`

| Endpoint | Метод | Параметры | Описание |
|----------|-------|-----------|----------|
| `/simple/price` | GET | ids, vs_currencies, x_cg_demo_api-key | Цены криптовалют |
| `/coins/list` | GET | x_cg_demo_api-key | Список всех монет |
| `/simple/supported_vs_currencies` | GET | - | Поддерживаемые валюты |
| `/search/trending` | GET | - | Трендовые монеты |
| `/coins/markets` | GET | vs_currency, order, per_page, page | Топ по капитализации |
| `/coins/{id}` | GET | localization, tickers, market_data... | Детали монеты |
| `/coins/{id}/market_chart` | GET | vs_currency, days | График цен |

**Примеры запросов:**

1. **Получить цену Bitcoin в USD:**
```
GET /simple/price?ids=bitcoin&vs_currencies=usd&x_cg_demo_api-key=YOUR_KEY
```

2. **Топ-10 монет:**
```
GET /coins/markets?vs_currency=usd&order=market_cap_desc&per_page=10&page=1
```

3. **Трендовые монеты:**
```
GET /search/trending
```

### 11.14 Переменные окружения (.env)

```env
TELEGRAM_TOKEN=your_telegram_bot_token_here
```

**Важно:**
- Файл `.env` должен быть в корне проекта
- НЕ коммитьте `.env` в репозиторий (добавлен в `.gitignore`)
- Формат токена: `123456789:ABCDEF1234567890abcdef1234567890abc`

### 11.15 Безопасность

**Рекомендации:**

1. **Хранение секретов:**
   - Используйте `.env` для токенов и ключей
   - Никогда не публикуйте `.env` в открытых репозиториях

2. **API-ключи пользователей:**
   - Хранятся в SQLite в открытом виде (для production рекомендуется шифрование)
   - Каждый пользователь использует свой ключ

3. **Rate limiting:**
   - CoinGecko Demo API имеет лимиты (обычно 10-30 запросов/минуту)
   - Используйте встроенный кеш для уменьшения количества запросов
   - Не делайте частые запросы без необходимости

4. **Валидация входных данных:**
   - Проверяйте корректность параметров команд
   - Используйте `validate_currency()` для проверки валют

### 11.16 Производительность

**Оптимизации:**

1. **Кеширование:**
   - HTTP-ответы кешируются на 10 секунд
   - Уменьшает нагрузку на API и ускоряет ответы

2. **Асинхронность:**
   - Все I/O операции асинхронные (aiohttp, aiogram)
   - Бот может обрабатывать множество пользователей одновременно

3. **Фоновые задачи:**
   - Проверка алертов не блокирует основной поток
   - Используется `asyncio.create_task()`

**Рекомендации для масштабирования:**

- Для большого количества пользователей переключитесь на PostgreSQL
- Рассмотрите использование Redis для кеширования
- Внедрите rate limiting на уровне бота
- Используйте webhook вместо polling для production

### 11.17 Тестирование

**Ручное тестирование команд:**

1. Запустите бота: `python bot.py`
2. Отправьте `/start` в Telegram
3. Протестируйте каждую команду:
   - `/price bitcoin usd`
   - `/convert bitcoin ethereum 1`
   - `/coin bitcoin`
   - `/alert bitcoin выше 100000 usd`
   - `🔔 Мои алерты`
   - `/alert_remove 1`

**Проверка базы данных:**

```bash
# Открыть SQLite
sqlite3 users.db

# Посмотреть пользователей
SELECT * FROM coingecko;

# Посмотреть алерты
SELECT * FROM alerts;

# Выход
.quit
```

**Просмотр логов:**

Логи выводятся в консоль с уровнем DEBUG. Ищите:
- Ошибки HTTP запросов
- Попадания/промахи кеша
- Срабатывания алертов

### 11.18 Развёртывание

**Локальное развёртывание:**

1. Клонируйте репозиторий
2. Создайте виртуальное окружение
3. Установите зависимости
4. Настройте `.env`
5. Запустите: `python bot.py`

**Развёртывание на сервере (Linux):**

```bash
# 1. Установите зависимости
sudo apt update
sudo apt install python3 python3-pip python3-venv git

# 2. Клонируйте проект
git clone https://github.com/yourusername/CryptoBot.git
cd CryptoBot

# 3. Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Настройте .env
nano .env
# Добавьте TELEGRAM_TOKEN=...

# 6. Запустите в screen/tmux
screen -S cryptobot
python bot.py
# Ctrl+A, D для отключения
```

**Автозапуск через systemd (опционально):**

Создайте файл `/etc/systemd/system/cryptobot.service`:

```ini
[Unit]
Description=CryptoBot Telegram Bot
After=network.target

[Service]
Type=simple
User=yourusername
WorkingDirectory=/path/to/CryptoBot
Environment="PATH=/path/to/CryptoBot/.venv/bin"
ExecStart=/path/to/CryptoBot/.venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable cryptobot
sudo systemctl start cryptobot
sudo systemctl status cryptobot
```

### 11.19 Мониторинг и отладка

**Проверка работы бота:**

```bash
# Просмотр логов systemd
sudo journalctl -u cryptobot -f

# Просмотр процесса
ps aux | grep bot.py

# Проверка использования ресурсов
top -p $(pидof python)
```

**Отладка проблем:**

1. **Бот не отвечает:**
   - Проверьте интернет-соединение
   - Проверьте корректность токена в `.env`
   - Посмотрите логи на наличие ошибок

2. **Алерты не работают:**
   - Убедитесь, что `alerts_worker` запущен
   - Проверьте наличие алертов в БД
   - Проверьте правильность API-ключа пользователя

3. **Ошибки CoinGecko API:**
   - Проверьте rate limits
   - Убедитесь, что API-ключ активен
   - Проверьте правильность названий монет

### 11.20 Changelog и версионирование

**Текущая версия:** 1.0.0

**Будущие улучшения:**
- AI-ассистент для анализа рынка
- Графики и визуализация данных
- Экспорт истории в CSV/JSON
- Webhook режим вместо polling
- Уведомления о волатильности
- Портфолио трекинг
- Интеграция с DeFi протоколами

## Контакты и поддержка

При возникновении вопросов или проблем:
1. Проверьте раздел **"Потенциальные ошибки и их устранение"** выше
2. Посмотрите **Техническую документацию** (раздел 11)
3. Проверьте логи консоли при запуске бота
4. Убедитесь, что все зависимости установлены: `pip list`

---

**Разработчик:** Tamirlanv  
**Репозиторий:** https://github.com/Tamirlanv/CryptoBot  
**Лицензия:** MIT
